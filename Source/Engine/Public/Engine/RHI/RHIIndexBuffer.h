#ifndef __volucris_rhi_index_buffer_h__
#define __volucris_rhi_index_buffer_h__

#include <Engine/RHI/RHIBuffer.h>

namespace volucris
{
	/*class RHIIndexBuffer : public RHIBuffer
	{
	public:
		RHIIndexBuffer(size_t size, EBufferUsage usage = StaticDraw);

		bool init(RHICommandList* command) override { return false; }

		bool init(RHICommandList* command, const std::vector<uint8>& data);

	private:

	};*/
}

#endif // !__volucris_rhi_index_buffer_h__
